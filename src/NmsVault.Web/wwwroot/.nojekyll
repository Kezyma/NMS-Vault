Without this file GitHub Pages runs the published output through Jekyll, which drops every
directory whose name begins with an underscore. _framework is one of them, and it is the
entire application - the symptom is a blank page with no error anywhere obvious.
